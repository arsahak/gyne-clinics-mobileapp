# assets

Put your app assets here:

- `icon.png` — 1024×1024
- `adaptive-icon.png` — 1024×1024 (Android foreground)
- `splash.png` — 1242×2436 or similar tall image

Until you add them, replace the paths in `app.json` with `null` or use placeholder images.
